services:
  telegram_radar:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    env_file:
      - .env
    volumes:
      - tg_data:/app/data
    restart: unless-stopped

volumes:
  tg_data:
